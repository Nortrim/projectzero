mixin skills-mixin(name, checked)
    .skill-wrapper
      span.skill__name= name
      br
      .skill__skill-box
        - var i = 0
        while i < 12
          if (i < checked)
            div(class='skill-circle skill__condition_checked')
            - i++
          else
            .skill-circle
            - i++
